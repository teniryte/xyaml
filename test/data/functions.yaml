name: Denis Ivanov
age: 29
nextAge: ${age + 1}
welcome: Hello
getHello: (name) => {
  return `${welcome}, ${name}!`;
};

math:

  numbers:
    - ${add(1, 2)}
    - ${add(1, 3)}
    - ${add(1, 4)}
    - ${add(1, 5)}
    - ${add(1, 6)}

  sum: ${add(55, 44)}

  add: (a, b) => {
    return a + b;
  };
